# Telegram Text-to-Speech Bot

This is a simple Telegram bot that converts text messages into speech and returns them as an MP3 file.

## Requirements

- Python 3.10+
- `python-telegram-bot`
- `gTTS`

## Installation

1. Install dependencies:
   ```bash
   pip install python-telegram-bot gtts
